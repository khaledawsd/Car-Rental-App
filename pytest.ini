[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning
    # Flask-Limiter warns that memory:// is not for production. It is the right
    # default for local runs, and tests disable the limiter anyway.
    ignore:Using the in-memory storage:UserWarning
